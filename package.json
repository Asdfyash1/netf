{
  "name": "zip-deployer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "node unzip.js",
    "build": "cd app && npm install --save better-sqlite3 @prisma/adapter-better-sqlite3 && npx prisma generate && next build",
    "start": "cd app && next start -p $PORT"
  },
  "dependencies": {
    "adm-zip": "^0.5.16"
  }
}
